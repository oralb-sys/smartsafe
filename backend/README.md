# SmartSafe Backend

Backend REST de SmartSafe construido con FastAPI.

## Requisitos

- Python 3.12

## Instalación

Desde la carpeta `backend`, crear un entorno virtual:

```bash
python -m venv .venv